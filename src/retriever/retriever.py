def get_similar_responses(question: str) -> list:
    # TODO: Implement the logic to get the similar responses
    
    # Step 1: Convert question to embedding
    # Step 2: Compute similarity if Question to knowledge base (ideally aprrox nearest neighbor index, HNSW)
    # Step 3: Prune top K 
    # Step 4: Get the raw text
    # Step 5: Format the output
    
    return ["These are test responses"]
